# When empty, the frontend uses relative URLs (same-origin via API gateway).
# Set this only for local development when frontend runs on a separate port.
# VITE_API_BASE_URL=http://localhost:8080
VITE_API_BASE_URL=
